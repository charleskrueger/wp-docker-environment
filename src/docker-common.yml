nginx:
  build: nginx
  ports:
    - "80:80"

webapp:
  build: webapp
  env_file:
    - db/secrets.env
  ports:
    - "8000:8000"

db:
  build: db
  env_file:
    - db/secrets.env

dbdata:
  build: dbdata
